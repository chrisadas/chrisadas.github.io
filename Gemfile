source "https://rubygems.org"

# Matches the exact gem versions GitHub Pages runs server-side, so a local
# preview behaves the same as production. Only needed if you ever want to
# run the site locally with `bundle exec jekyll serve`.
gem "github-pages", group: :jekyll_plugins

# Plugins
group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

# Windows / JRuby compatibility shims (harmless elsewhere)
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]
gem "wdm", "~> 0.1.1", platforms: [:mingw, :mswin, :x64_mingw]
